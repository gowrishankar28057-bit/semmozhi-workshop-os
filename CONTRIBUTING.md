# Contributing

Create a focused branch, update docs and tests with behavior, and run `npm run db:validate`, `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`. Never commit local environment files. Pull requests must state whether work is implemented, scaffolded, or planned and note RBAC/data migration impacts.
